# NodeJS SDK multisig submit transaction example

In this example we convert address in one format to another

## Prerequisite

-   Node.js >= [14.x installed](https://nodejs.org)


## Install packages & run:

```sh
npm install
node index.js 
```
